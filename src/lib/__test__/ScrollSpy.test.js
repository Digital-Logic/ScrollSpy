import React from 'react';
import { mount } from 'enzyme';
import ScrollSpy from '../ScrollSpy';
import '../../setupTest';

jest.useFakeTimers();

describe("Scroll Spy Test", () => {
    it('Should mount', () => {

    });
});